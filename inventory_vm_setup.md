```
pip install "apache-airflow==2.7.3" --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.7.3/constraints-3.8.txt"
sudo apt remove python3-pip 
wget https://bootstrap.pypa.io/get-pip.py
sudo python3 get-pip.py

pip install pyopenssl --upgrade
python3 -m pip --version # check pip version

git config --global --edit #edit configuration file

git commit --amend --reset-author # fix the identity used for the commit

You can view all of your settings and where they are coming from using:

$ git config --list --show-origin

$ sudo apt install python3.8-venv
```
